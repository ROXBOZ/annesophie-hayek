import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'rgci878b',
    dataset: 'production',
  },
  autoUpdates: true,
  studioHost: 'annesophie-hayek',
})
